import { CommonMedia } from '../entrypoint/utils/media';
export declare function normalizeTitle(title: string): string;
export declare function compareTitle(a: string, b: string): boolean;
export declare function compareMedia(media: CommonMedia, title: string, releaseYear?: number): boolean;
